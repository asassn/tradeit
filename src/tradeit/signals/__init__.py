"""Signal research: which indicators actually predict anything.

A measurement, not a display feature. ``study`` holds the machinery; the
discipline it enforces is that a study declares its target before it runs, and
that being statistically detectable is not the same as being worth trading.
"""
